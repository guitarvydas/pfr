#!/bin/bash

set -e
trap 'catch' ERR

catch () {
    exit 1
}

# usage: pf source-file grammar-file rewrite-file
pdir=/app/pfr
support=`realpath $4`
tflag=$5
vflag=$6
docker run util pfr $1 $2 $3 $support $tflga $vflag
